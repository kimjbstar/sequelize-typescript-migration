## What does this change, and why?

Fixes #

## How was this tested?

-   [ ] `npm test` passes
-   [ ] `npm run build` passes
-   [ ] Added or updated a test for this change (see [CONTRIBUTING.md](../CONTRIBUTING.md) for the pattern)
-   [ ] If the generated migration changed intentionally, I reviewed the `src/fixtures.golden.spec.ts` snapshot diff (`npx jest -u`) before committing it
-   [ ] If this touches dialect-specific behaviour, I ran `npm run test:integration` against a real database

## Anything reviewers should pay extra attention to?
